# yaml-language-server: $schema=https://raw.githubusercontent.com/LeShaunJ/ops-schema/main/ops.schema.yaml
revision: 2
dependencies:
  apk:
    - less
  apt:
    - less
  brew:
    - less
  cask:
    - less
  custom:
    - echo "hello, world"
  dir:
    - config/{dev,staging,production}
  docker:
    - ubuntu/nginx
  gem:
    - ejson
  terraform:
    - dev
actions:
  myaction:
    command: echo "hello, world"
options:
  gem:
    user_install: true
    use_sudo: false
