# yaml-language-server: $schema=../../../ops.schema.json
revision: 2
dependencies:
  apk:
    - less
  apt:
    - less
  brew:
    - less
  cask:
    - less
  custom:
    - echo "hello, world"
  dir:
    - config/{dev,staging,production}
  docker:
    - ubuntu/nginx
  gem:
    - ejson
  terraform:
    - dev
actions:
  myaction:
    command: echo "hello, world"
options:
  gem:
    user_install: true
    use_sudo: false
